services:
  - type: web
    name: fastapi-service
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: "./start.sh"
    envVars:
      - key: ENVIRONMENT
        value: production
